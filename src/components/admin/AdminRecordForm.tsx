'use client';
import { FormEvent,useState } from "react";
import { useRouter } from "next/navigation";
const booleanFields=new Set(["is_active","is_featured","is_published"]); const dateFields=new Set(["start_date","due_date"]); const jsonFields=new Set(["features","pricing","faqs"]); const optional=new Set(["email","phone","company_name","message","source","description","short_description","image_url","project_url","service_id","category_id","notes","start_date","due_date","icon","hero_title","hero_description","cta_label","features","pricing","faqs","sort_order"]);
export default function AdminRecordForm({resource,fields,initial={}}:{resource:string;fields:string[];initial?:Record<string,unknown>}){const router=useRouter();const [values,setValues]=useState<Record<string,string>>(()=>Object.fromEntries(fields.map(f=>[f,String(initial[f]??(booleanFields.has(f)?"true":""))])));const [error,setError]=useState("");const [saving,setSaving]=useState(false);async function submit(e:FormEvent){e.preventDefault();setSaving(true);setError("");const payload:Record<string,unknown>={};for(const field of fields)payload[field]=booleanFields.has(field)?values[field]==="true":values[field]||null;const id=initial.id?`/${initial.id}`:"";const res=await fetch(`/api/admin/${resource}${id}`,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(payload)});if(!res.ok){const data=await res.json().catch(()=>({}));setError(data.error||"Unable to save record.");setSaving(false);return;}router.push(`/admin/${resource}`);router.refresh();}
return <form onSubmit={submit} className="mt-8 space-y-5 rounded-3xl border border-white/10 bg-white/[0.04] p-6">{fields.map(field=><label key={field} className="block"><span className="mb-2 block text-sm font-medium text-slate-300">{field.replaceAll("_"," ")}</span>{booleanFields.has(field)?<select value={values[field]} onChange={e=>setValues({...values,[field]:e.target.value})} className="w-full rounded-xl border border-white/10 bg-slate-900 px-4 py-3"><option value="true">Yes</option><option value="false">No</option></select>:jsonFields.has(field)?<textarea rows={field==="features"?5:8} placeholder={field==="features"?'["Feature one","Feature two"]':field==="pricing"?'[{"name":"Starter","description":"...","price":"Custom"}]':'[{"q":"Question?","a":"Answer."}]'} value={values[field]} onChange={e=>setValues({...values,[field]:e.target.value})} className="w-full rounded-xl border border-white/10 bg-slate-900 px-4 py-3 text-white outline-none focus:border-blue-500"/>:<input required={!optional.has(field)} type={dateFields.has(field)?"date":field==="amount"?"number":"text"} step={field==="amount"?"0.01":undefined} value={values[field]} onChange={e=>setValues({...values,[field]:e.target.value})} className="w-full rounded-xl border border-white/10 bg-slate-900 px-4 py-3 text-white outline-none focus:border-blue-500"/>}</label>)}{error&&<p className="rounded-xl border border-red-500/20 bg-red-500/10 p-3 text-sm text-red-300">{error}</p>}<button disabled={saving} className="rounded-xl bg-blue-600 px-5 py-3 font-semibold hover:bg-blue-500 disabled:opacity-50">{saving?"Saving…":initial.id?"Save changes":"Create record"}</button></form>;}
