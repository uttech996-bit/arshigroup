'use client'; import {useState} from 'react';
const roles=['client','super_admin','admin','manager','support_agent','editor']; export default function RoleControl({id,role}:{id:string;role:string}){const[r,setR]=useState(role);const[s,setS]=useState('');async function change(v:string){setR(v);setS('Saving…');const res=await fetch(`/api/admin/users/${id}/role`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({role:v})});setS(res.ok?'Saved':'Not permitted');}return <div className="flex items-center gap-3"><select value={r} onChange={e=>change(e.target.value)} className="rounded-xl border border-white/10 bg-slate-900 px-3 py-2 text-sm"><option disabled>Role</option>{roles.map(x=><option key={x}>{x}</option>)}</select>{s&&<span className="text-xs text-slate-500">{s}</span>}</div>}
