'use client';
import {FormEvent,useState} from "react";
import {useRouter} from "next/navigation";
export default function TicketReply({ticketId}:{ticketId:string}){const router=useRouter();const [body,setBody]=useState("");const [saving,setSaving]=useState(false);async function submit(e:FormEvent){e.preventDefault();if(!body.trim())return;setSaving(true);const r=await fetch(`/api/tickets/${ticketId}/messages`,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({body})});if(r.ok){setBody("");router.refresh();}setSaving(false);}return <form onSubmit={submit} className="mt-8 flex gap-3"><textarea value={body} onChange={e=>setBody(e.target.value)} rows={3} placeholder="Write a reply…" className="min-w-0 flex-1 rounded-2xl border border-border bg-background p-4 outline-none focus:border-blue-500"/><button disabled={saving} className="self-end rounded-xl bg-blue-600 px-5 py-3 font-bold text-white disabled:opacity-50">{saving?"Sending…":"Reply"}</button></form>}
