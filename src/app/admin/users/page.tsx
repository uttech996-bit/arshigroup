import {createClient} from "@/lib/supabase/server"; import RoleControl from "@/components/admin/RoleControl";
export default async function UsersPage(){const s=await createClient();const{data:rows}=await s.from('profiles').select('id,full_name,company_name,phone,role,created_at').order('created_at',{ascending:false});return <main className="min-h-screen bg-slate-950 px-5 py-10 text-white"><div className="mx-auto max-w-6xl"><h1 className="text-4xl font-black">Users & roles</h1><p className="mt-2 text-slate-400">Role changes are restricted to Super Admin and enforced in the database.</p><div className="mt-8 space-y-3">{(rows??[]).map(r=><div key={r.id} className="flex flex-wrap items-center justify-between gap-4 rounded-2xl border border-white/10 bg-white/[.04] p-5"><div><p className="font-bold">{r.full_name||'Unnamed user'}</p><p className="text-sm text-slate-400">{r.company_name||'No company'} · {r.phone||'No phone'}</p></div><RoleControl id={r.id} role={r.role}/></div>)}</div></div></main>}
