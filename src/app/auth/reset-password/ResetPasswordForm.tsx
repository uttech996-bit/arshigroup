'use client';
import { FormEvent,useState } from "react";
import { useRouter } from "next/navigation";
import { createClient } from "@/lib/supabase/client";
export default function ResetPasswordForm(){const r=useRouter();const[p,setP]=useState("");const[c,setC]=useState("");const[e,setE]=useState("");const[l,setL]=useState(false);async function submit(x:FormEvent){x.preventDefault();setE("");if(p.length<8||p!==c){setE(p.length<8?"Password must be at least 8 characters.":"Passwords do not match.");return}setL(true);const{error}=await createClient().auth.updateUser({password:p});if(error)setE(error.message);else r.replace("/dashboard");setL(false)}return <form onSubmit={submit} className="mt-8 space-y-4"><input type="password" required minLength={8} autoComplete="new-password" value={p} onChange={e=>setP(e.target.value)} placeholder="New password" className="w-full rounded-xl border border-white/10 bg-black/20 px-4 py-3 outline-none focus:border-blue-500"/><input type="password" required minLength={8} autoComplete="new-password" value={c} onChange={e=>setC(e.target.value)} placeholder="Confirm password" className="w-full rounded-xl border border-white/10 bg-black/20 px-4 py-3 outline-none focus:border-blue-500"/>{e&&<p role="alert" className="rounded-xl bg-red-500/10 p-3 text-sm text-red-300">{e}</p>}<button disabled={l} className="w-full rounded-xl bg-blue-600 px-4 py-3 font-semibold disabled:opacity-60">{l?"Updating…":"Update password"}</button></form>}
