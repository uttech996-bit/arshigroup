'use client';
import { useState } from "react";
import { createClient } from "@/lib/supabase/client";
export default function VerifyEmailPage(){const[e,setE]=useState("");const[m,setM]=useState("");const[l,setL]=useState(false);async function resend(){setL(true);setE("");setM("");const s=createClient();const{data:{user}}=await s.auth.getUser();if(!user?.email){setE("Please sign up or sign in first.");setL(false);return}const{error}=await s.auth.resend({type:"signup",email:user.email});if(error)setE(error.message);else setM("Verification email sent. Check your inbox and spam folder.");setL(false)}return <main className="min-h-screen bg-slate-950 px-6 py-20 text-white"><div className="mx-auto max-w-md rounded-3xl border border-white/10 bg-white/[0.04] p-8 shadow-2xl"><p className="text-sm font-semibold uppercase tracking-[0.3em] text-blue-400">ARSHI GROUP</p><h1 className="mt-4 text-3xl font-bold">Verify your email</h1><p className="mt-3 text-slate-400">Confirm your email address to access your private workspace.</p><button onClick={resend} disabled={l} className="mt-8 w-full rounded-xl bg-blue-600 px-4 py-3 font-semibold disabled:opacity-60">{l?"Sending…":"Resend verification email"}</button>{e&&<p className="mt-4 rounded-xl bg-red-500/10 p-3 text-sm text-red-300">{e}</p>}{m&&<p className="mt-4 rounded-xl bg-emerald-500/10 p-3 text-sm text-emerald-300">{m}</p>}</div></main>}
