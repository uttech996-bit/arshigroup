'use client';
import {FormEvent,useState} from "react";
import {useRouter} from "next/navigation";
export default function NewTicket(){const router=useRouter();const [subject,setSubject]=useState("");const [description,setDescription]=useState("");const [priority,setPriority]=useState("normal");const [error,setError]=useState("");const [saving,setSaving]=useState(false);async function submit(e:FormEvent){e.preventDefault();setSaving(true);const r=await fetch("/api/tickets",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({subject,description,priority})});const d=await r.json();if(!r.ok){setError(d.error||"Unable to create ticket");setSaving(false);return}router.push(`/dashboard/tickets/${d.id}`);router.refresh()}return <div className="section-wrap !py-10"><h1 className="text-4xl font-black">Open a support ticket</h1><p className="mt-2 text-muted-foreground">Tell our team what you need help with.</p><form onSubmit={submit} className="mt-8 max-w-2xl rounded-3xl border border-border bg-card p-6 space-y-5"><input required value={subject} onChange={e=>setSubject(e.target.value)} placeholder="Subject" className="w-full rounded-xl border border-border bg-background px-4 py-3 outline-none focus:border-blue-500"/><select value={priority} onChange={e=>setPriority(e.target.value)} className="w-full rounded-xl border border-border bg-background px-4 py-3"><option value="low">Low priority</option><option value="normal">Normal priority</option><option value="high">High priority</option><option value="urgent">Urgent</option></select><textarea required rows={7} value={description} onChange={e=>setDescription(e.target.value)} placeholder="Describe the issue or request…" className="w-full rounded-xl border border-border bg-background px-4 py-3 outline-none focus:border-blue-500"/>{error&&<p className="text-sm text-red-500">{error}</p>}<button disabled={saving} className="rounded-xl bg-blue-600 px-5 py-3 font-bold text-white">{saving?"Creating…":"Create ticket"}</button></form></div>}
