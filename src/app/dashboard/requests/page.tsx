import Link from "next/link"; import { createClient } from "@/lib/supabase/server";
export default async function Requests(){const s=await createClient();const{data}=await s.from('service_requests').select('id,status,budget,message,created_at,services(name,slug)').order('created_at',{ascending:false});return <div className="section-wrap !py-10"><div className="flex flex-wrap items-end justify-between gap-4"><div><h1 className="text-3xl font-black">Service requests</h1><p className="mt-2 text-muted-foreground">Track requests submitted to ARSHI GROUP.</p></div><Link href="/services" className="rounded-xl bg-blue-600 px-4 py-2.5 text-sm font-bold text-white">New request</Link></div><div className="mt-8 space-y-3">{(data??[]).map((x:any)=><div key={x.id} className="rounded-2xl border border-border bg-card p-5"><div className="flex justify-between gap-4"><div><p className="font-bold">{x.services?.name||'Service request'}</p><p className="mt-1 text-sm text-muted-foreground">{x.message||'No additional message.'}</p></div><span className="h-fit rounded-full bg-accent px-3 py-1 text-xs font-bold">{x.status}</span></div><p className="mt-4 text-xs text-muted-foreground">{new Date(x.created_at).toLocaleString()}</p></div>)}{!data?.length&&<div className="rounded-2xl border border-dashed border-border p-8 text-sm text-muted-foreground">No service requests yet.</div>}</div></div>}
