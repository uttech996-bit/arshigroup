import Link from "next/link"; import { createClient } from "@/lib/supabase/server";
export default async function Services(){const s=await createClient();const{data}=await s.from('services').select('id,name,slug,short_description,category_id').eq('is_active',true).order('sort_order');return <div className="section-wrap !py-10"><h1 className="text-3xl font-black">Services</h1><p className="mt-2 text-muted-foreground">Explore ARSHI GROUP services and start a private request.</p><div className="mt-8 grid gap-4 md:grid-cols-2">{(data??[]).map(x=><Link key={x.id} href={`/services/${x.slug}`} className="rounded-2xl border border-border bg-card p-6 hover:border-blue-500/40"><h2 className="text-xl font-bold">{x.name}</h2><p className="mt-2 text-sm leading-6 text-muted-foreground">{x.short_description||'Professional service delivered by ARSHI GROUP.'}</p><span className="mt-5 inline-block text-sm font-bold text-blue-600">View service →</span></Link>)}</div></div>}
