import { createClient } from "@/lib/supabase/server";
export default async function Activity(){const s=await createClient();const{data}=await s.from('activity_logs').select('id,action,entity_type,created_at,metadata').order('created_at',{ascending:false}).limit(50);return <div className="section-wrap !py-10"><h1 className="text-3xl font-black">Activity</h1><p className="mt-2 text-muted-foreground">A private history of activity on your account.</p><div className="mt-8 space-y-2">{(data??[]).map(x=><div key={x.id} className="rounded-xl border border-border bg-card p-4"><p className="font-semibold">{x.action}</p><p className="mt-1 text-xs text-muted-foreground">{x.entity_type||'account'} · {new Date(x.created_at).toLocaleString()}</p></div>)}</div></div>}
